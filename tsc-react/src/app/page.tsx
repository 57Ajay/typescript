import Button from "@/components/Button"
const Home = () => {
  return (
    <div>
      <main className="min-h-screen flex justify-center items-center">
        <Button style={{backgroundColor: "blue", textColor: "red", fontSize: 12}}/>
      </main>
    </div>
  )
}

export default Home